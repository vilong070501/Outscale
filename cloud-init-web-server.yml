#cloud-config
package_update: true
package_upgrade: true
packages:
  - nginx

runcmd:
  - systemctl start nginx
  - systemctl enable nginx
  - echo "<h1>Bienvenue sur le serveur Web $(hostname)</h1>" > /var/www/html/index.html